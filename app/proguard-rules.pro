# RepoGlance — no custom ProGuard rules yet. isMinifyEnabled is false for
# release in Slice 1; this file exists so the release buildType's
# proguardFiles() reference resolves once minification is turned on later.
