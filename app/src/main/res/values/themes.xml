<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 31-33 fallback. Material 3 computes the app's dynamic
         background there with no system resource, so this neutral only
         approximates it and a small tone step may show. API 34+ matches
         exactly via values-v34. -->
    <style name="Theme.RepoGlance" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/system_neutral1_10</item>
    </style>
</resources>
